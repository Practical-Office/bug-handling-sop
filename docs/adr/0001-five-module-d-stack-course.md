# Five-Module D-stack course as first publish

Greenfield audience (team has not taken the class). We chose a full first publish: Living SOP rewrite + five content modules (Report → Triage → Diagnosis → Fix → Review) with hub “Why”, Setup pre-req, in-repo Exercise Lab, Evidence Pack certification, workshop agenda, and thin incident/postmortem rules — rather than an interim 4-module B-phase. Diagnosis workflow follows Matt’s `/diagnosing-bugs` six phases; severity and priority are separate label sets (`severity/s1`–`s4`, `priority/p0`–`p3`); triage adopts Matt’s state machine plus peer extras.
